<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Homepage</title>
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
      <% for (const message of messages) { %>
      <li>[<%= message.added %>] <%= message.user %> says: <%= message.text %></li>
      <% } %>
    </ul>
  </body>
</html>